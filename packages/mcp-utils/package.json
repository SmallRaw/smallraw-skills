{
  "name": "mcp-utils",
  "version": "0.1.0",
  "description": "Lazy MCP client CLI — connect, call, disconnect on demand via registry",
  "bin": {
    "mcp-utils": "./dist/mcp-utils.cjs"
  },
  "files": [
    "dist/mcp-utils.cjs",
    "mcp-registry.schema.json"
  ],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node18 --format=cjs --outfile=dist/mcp-utils.cjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "esbuild": "^0.25.12",
    "typescript": "^5.7.0"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cli"
  ],
  "license": "MIT"
}
